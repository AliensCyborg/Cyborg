# PyOS (Reserved Domain)

This folder reserves ACC workflow namespace for **PyOS ecosystem**.

Goals:
- Avoid future collisions with WebOS / DartOS / SharpOS.
- Keep WorkflowIds deterministic and prefixed: `PyOS_*`.

Status: Reserved (no active execution workflows registered yet).
